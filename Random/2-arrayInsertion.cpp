#include "iostream"
using namespace std;

int main() {
  int len = 10;
  int arr[len];

  int index;
  int value;
  cin >> value >> index;

  if (index > 9 || index < 0) {
    cout << "Out of range\n";
  } else {
  }
}